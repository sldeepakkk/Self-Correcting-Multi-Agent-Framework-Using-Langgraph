# Environment
.venv/
.env
__pycache__/
*.pyc

# Cache — regenerates automatically
cache/cache.index
cache/cache_store.json

# Vector store — regenerates from seed scripts
data/vector_store.index
data/vector_store_docs.json

# Run logs — too large, not useful to readers
run_logs/

# Episodic memory — personal to your runs
memory/episodic.db

# Benchmark outputs — keep the plots, skip raw JSON
benchmarks/results/summary.json
benchmarks/run_benchmark.py
benchmarks/plain_llm.py
benchmarks/results
memory/MEMORY_LOG.md

app.py